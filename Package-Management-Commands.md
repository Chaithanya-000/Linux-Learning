# Package Management Commands

## apt

Purpose:
Package manager ni access cheyyadaniki.

Syntax:
apt <command>

Example:
apt list

Output:
Available packages information display avuthundi.

---

## apt update

Purpose:
Package repository information update cheyyadaniki.

Syntax:
apt update

Example:
apt update

Output:
Latest package information retrieve avuthundi.

---

## apt upgrade

Purpose:
Installed packages ni latest versions ki update cheyyadaniki.

Syntax:
apt upgrade

Example:
apt upgrade

Output:
Installed packages update avuthayi.

---

## apt install

Purpose:
New package install cheyyadaniki.

Syntax:
apt install <package_name>

Example:
apt install nginx

Output:
nginx package install avuthundi.

---

## apt remove

Purpose:
Installed package remove cheyyadaniki.

Syntax:
apt remove <package_name>

Example:
apt remove nginx

Output:
nginx package remove avuthundi.

---

## apt search

Purpose:
Package search cheyyadaniki.

Syntax:
apt search <package_name>

Example:
apt search nginx

Output:
Matching packages display avuthayi.

---

## apt show

Purpose:
Package details chudadaniki.

Syntax:
apt show <package_name>

Example:
apt show nginx

Output:
Package version, size, description display avuthayi.

---

## dpkg -i

Purpose:
Downloaded .deb package install cheyyadaniki.

Syntax:
dpkg -i <package_name.deb>

Example:
dpkg -i app.deb

Output:
.deb package install avuthundi.

---

## dpkg -l

Purpose:
Installed packages list chudadaniki.

Syntax:
dpkg -l

Example:
dpkg -l

Output:
Installed packages list display avuthundi.

---

## yum

Purpose:
RHEL/CentOS systems lo packages manage cheyyadaniki.

Syntax:
yum <command>

Example:
yum install nginx

Output:
nginx package install avuthundi.

---

## dnf

Purpose:
Modern RHEL/Fedora systems lo package management kosam.

Syntax:
dnf <command>

Example:
dnf install nginx

Output:
nginx package install avuthundi.

---

## rpm -i

Purpose:
RPM package install cheyyadaniki.

Syntax:
rpm -i <package_name.rpm>

Example:
rpm -i app.rpm

Output:
RPM package install avuthundi.

---

## rpm -qa

Purpose:
Installed RPM packages list chudadaniki.

Syntax:
rpm -qa

Example:
rpm -qa

Output:
Installed RPM packages display avuthayi.
