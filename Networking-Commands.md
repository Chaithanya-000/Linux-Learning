# Networking Commands

## ping

Purpose:
Network connectivity test cheyyadaniki.

Syntax:
ping <host_name_or_ip>

Example:
ping google.com

Output:
Packets send and receive details display avuthayi.

---

## ip addr

Purpose:
System IP address information chupisthundi.

Syntax:
ip addr

Example:
ip addr

Output:
Network interfaces and IP addresses display avuthayi.

---

## ip route

Purpose:
Routing table information chupisthundi.

Syntax:
ip route

Example:
ip route

Output:
Default gateway and routing details display avuthayi.

---

## ifconfig

Purpose:
Network interface information chupisthundi.

Syntax:
ifconfig

Example:
ifconfig

Output:
IP address, subnet mask, interface details display avuthayi.

Note:
Modern Linux systems lo ip command ekkuva use chestharu.

---

## netstat

Purpose:
Network connections and ports chupisthundi.

Syntax:
netstat

Example:
netstat -tuln

Output:
Listening ports and active connections display avuthayi.

---

## ss

Purpose:
Socket and network connection information chupisthundi.

Syntax:
ss

Example:
ss -tuln

Output:
Open ports and active connections display avuthayi.

---

## curl

Purpose:
URL nundi data retrieve cheyyadaniki.

Syntax:
curl <url>

Example:
curl https://google.com

Output:
Web page content display avuthundi.

---

## wget

Purpose:
Internet nundi files download cheyyadaniki.

Syntax:
wget <url>

Example:
wget https://example.com/file.zip

Output:
File current directory lo download avuthundi.

---

## nslookup

Purpose:
Domain name ni IP address ga resolve cheyyadaniki.

Syntax:
nslookup <domain>

Example:
nslookup google.com

Output:
Domain IP address details display avuthayi.

---

## dig

Purpose:
Detailed DNS information retrieve cheyyadaniki.

Syntax:
dig <domain>

Example:
dig google.com

Output:
DNS records and query details display avuthayi.

---

## traceroute

Purpose:
Destination server reach avvadanki packets travel chese route chupisthundi.

Syntax:
traceroute <host>

Example:
traceroute google.com

Output:
Packet travel path hop-by-hop display avuthundi.

---

## hostname

Purpose:
System hostname chupisthundi.

Syntax:
hostname

Example:
hostname

Output:
Current machine hostname display avuthundi.
